#include <stdio.h>
/**
 * main - prints "programming is like building a multilingual puzzle, followed by a newline."
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    puts("\programming is like building a multilingual puzzle, followed by a newline.");

    return(0);
}
}
